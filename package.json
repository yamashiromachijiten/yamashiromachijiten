{
  "name": "yamashiromachijiten",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "hugo server -s site -D --renderToMemory",
    "build": "hugo -s site -d ../dist --cleanDestinationDir"
  }
}
